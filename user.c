#include "oss.h"

int main(int argc, char* argv[]){

    sharedMemoryConfig();

    printf("Hello world\n");

    fprintf(fp, "I am from user...")

    shmdt(sysClockshmPtr);
    shmdt(PCBshmPtr);
    return 0;
}